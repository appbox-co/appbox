---
title: Kód
description: Kódok használatának elsajátítása az MDX fájlokban
links:
  source: https://raw.githubusercontent.com/rehype-pretty/rehype-pretty-code/master/examples/next/src/app/index.mdx
sort: 1
---

Az `opendocs` a [`rehype-pretty-code`](https://github.com/atomiks/rehype-pretty-code) könyvtárat használja
az MDX fájlokban található kódok kezelésére. Ez lehetővé teszi, hogy nagymértékben testreszabd a kódok megjelenését!

További információért nézd meg a hivatalos dokumentációt és példákat!

## Címek

**Példa:**

```js title="apps/web/src/index.tsx"
import { App } from './App';
```

**Kód:**

````mdx
```js title="apps/web/src/index.tsx"
import { App } from './App';
```
````

## Kódkiemelés

**Példa:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kód:**

````mdx
```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Beágyazott kód kiemelése

**Példa:**

A `[1, 2, 3].join('-'){:js}` eredménye `'1-2-3'{:js}`.

**Kód:**

```mdx
The result of `[1, 2, 3].join('-'){:js}` is `'1-2-3'{:js}`.
```

## Kontextusfüggő beágyazott kódkiemelés

Például ha a következő kódblokkod lenne:

```js
function getStringLength(str) {
  return str.length;
}
```

Amikor a `getStringLength{:.entity.name.function}` elemre függvényként hivatkozunk,
ennek megfelelően színezhetjük. Ugyanez igaz a `function{:.keyword}` elemre, vagy a
`str{:.variable.parameter}` és `str{:.variable.other.object}` közötti különbségre stb. Ez lehetővé teszi,
hogy szemantikusan összekapcsoljuk a beágyazott kódot a legközelebbi kódblokkal, amelyre hivatkozik.

**Kód:**
````mdx
When we refer to `getStringLength{:.entity.name.function}` as a function,
we can color it as such. Same with `function{:.keyword}`, or
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. This allows
semantically link inline code to the nearest block of code it refers to.
````

## Szókiemelés

**Példa:**

```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```

**Kód:**
````mdx
```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```
````

## Sorkiemelés

**Példa:**

```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kód:**

````mdx
```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Sorszámozás

**Példa:**

```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kód:**
````mdx
```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Diff

**Példa:**

```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

**Kód:**

````mdx
```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```
````

## ANSI kiemelés

**Példa:**

```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Beágyazott ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`

**Kód:**

````mdx
```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`
````

## Szócsoport-kiemelés azonosító alapján

Tegyél egy azonosítót a `#` után, a szavak után.
Ez lehetővé teszi, hogy a karaktereket eltérően színezd a megadott azonosító alapján.

**Példa:**

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

**Kód:**
````md
```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```
````

Stílusok módosításához vagy újak hozzáadásához:

```css title="apps/web/src/styles/mdx.css"
[data-chars-id='v'] {
  @apply !text-pink-300 bg-rose-800/50 font-bold;
}
```
