---
title: Kode
description: Menguasai kode dalam file MDX Anda
links:
  source: https://raw.githubusercontent.com/rehype-pretty/rehype-pretty-code/master/examples/next/src/app/index.mdx
sort: 1
---

`opendocs` menggunakan library [`rehype-pretty-code`](https://github.com/atomiks/rehype-pretty-code)
untuk menangani kode dalam file MDX. Ini memungkinkan Anda menyesuaikan tampilan kode secara luas!

Untuk informasi lebih lanjut, lihat dokumentasi dan contoh resminya!

## Judul

**Contoh:**

```js title="apps/web/src/index.tsx"
import { App } from './App';
```

**Kode:**

````mdx
```js title="apps/web/src/index.tsx"
import { App } from './App';
```
````

## Highlight kode

**Contoh:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kode:**

````mdx
```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Highlight kode inline

**Contoh:**

Hasil dari `[1, 2, 3].join('-'){:js}` adalah `'1-2-3'{:js}`.

**Kode:**

```mdx
The result of `[1, 2, 3].join('-'){:js}` is `'1-2-3'{:js}`.
```

## Highlight kode inline sadar konteks

Misalnya, jika Anda memiliki blok kode berikut:

```js
function getStringLength(str) {
  return str.length;
}
```

Saat kita merujuk `getStringLength{:.entity.name.function}` sebagai fungsi,
kita dapat mewarnainya sebagai fungsi. Begitu juga dengan `function{:.keyword}`, atau
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, dan seterusnya. Ini memungkinkan
kode inline terhubung secara semantik ke blok kode terdekat yang dirujuknya.

**Kode:**
````mdx
When we refer to `getStringLength{:.entity.name.function}` as a function,
we can color it as such. Same with `function{:.keyword}`, or
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. This allows
semantically link inline code to the nearest block of code it refers to.
````

## Highlight kata

**Contoh:**

```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```

**Kode:**
````mdx
```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```
````

## Highlight baris

**Contoh:**

```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kode:**

````mdx
```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Penomoran baris

**Contoh:**

```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Kode:**
````mdx
```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Diff

**Contoh:**

```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

**Kode:**

````mdx
```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```
````

## Highlight ANSI

**Contoh:**

```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

ANSI inline: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`

**Kode:**

````mdx
```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`
````

## Highlight grup kata berdasarkan id

Letakkan id setelah `#` setelah kata.
Ini memungkinkan Anda memberi warna berbeda pada karakter berdasarkan id yang diberikan.

**Contoh:**

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

**Kode:**
````md
```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```
````

Untuk mengubah atau menambahkan gaya baru:

```css title="apps/web/src/styles/mdx.css"
[data-chars-id='v'] {
  @apply !text-pink-300 bg-rose-800/50 font-bold;
}
```
