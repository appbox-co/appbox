---
title: Code
description: तुमच्या MDX files मधील code वापरण्यावर प्रभुत्व मिळवा.
links:
  source: https://raw.githubusercontent.com/rehype-pretty/rehype-pretty-code/master/examples/next/src/app/index.mdx
sort: 1
---

`opendocs` MDX files मधील code हाताळण्यासाठी [`rehype-pretty-code`](https://github.com/atomiks/rehype-pretty-code) library वापरते.
यामुळे code कसा दाखवला जातो हे तुम्ही मोठ्या प्रमाणात customize करू शकता.

अधिक माहितीसाठी official documentation आणि examples पहा.

## Titles

**उदाहरण:**

```js title="apps/web/src/index.tsx"
import { App } from './App';
```

**Code:**

````mdx
```js title="apps/web/src/index.tsx"
import { App } from './App';
```
````

## Code highlighting

**उदाहरण:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**

````mdx
```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Inline code highlighting

**उदाहरण:**

`[1, 2, 3].join('-'){:js}` चा result `'1-2-3'{:js}` आहे.

**Code:**

```mdx
The result of `[1, 2, 3].join('-'){:js}` is `'1-2-3'{:js}`.
```

## Context-aware inline code highlighting

उदाहरणार्थ, तुमच्याकडे खालील code block असेल:

```js
function getStringLength(str) {
  return str.length;
}
```

जेव्हा आपण `getStringLength{:.entity.name.function}` ला function म्हणून refer करतो,
तेव्हा त्याला तसे color करता येते. `function{:.keyword}` किंवा
`str{:.variable.parameter}` विरुद्ध `str{:.variable.other.object}` यांच्याबाबतीतही तेच लागू होते. यामुळे inline code ला तो ज्या जवळच्या code block ला refer करतो त्याच्याशी semantically link करता येते.

**Code:**
````mdx
When we refer to `getStringLength{:.entity.name.function}` as a function,
we can color it as such. Same with `function{:.keyword}`, or
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. This allows
semantically link inline code to the nearest block of code it refers to.
````

## Word highlighting

**उदाहरण:**

```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```

**Code:**
````mdx
```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```
````

## Line highlighting

**उदाहरण:**

```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**

````mdx
```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Line numbering

**उदाहरण:**

```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**
````mdx
```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Diff

**उदाहरण:**

```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

**Code:**

````mdx
```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```
````

## ANSI Highlight

**उदाहरण:**

```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`

**Code:**

````mdx
```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`
````

## Word group highlighting by id

Words नंतर `#` नंतर id ठेवा.
यामुळे दिलेल्या id नुसार characters वेगळ्या रंगात दाखवता येतात.

**उदाहरण:**

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

**Code:**
````md
```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```
````

नवीन styles बदलण्यासाठी किंवा जोडण्यासाठी:

```css title="apps/web/src/styles/mdx.css"
[data-chars-id='v'] {
  @apply !text-pink-300 bg-rose-800/50 font-bold;
}
```
