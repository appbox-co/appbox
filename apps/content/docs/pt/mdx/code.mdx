---
title: Código
description: Dominando blocos de código nos seus arquivos MDX
links:
  source: https://raw.githubusercontent.com/rehype-pretty/rehype-pretty-code/master/examples/next/src/app/index.mdx
sort: 1
---

O `opendocs` usa a biblioteca [`rehype-pretty-code`](https://github.com/atomiks/rehype-pretty-code)
para lidar com blocos de código em arquivos MDX. Isso permite personalizar bastante a forma como o código é exibido!

Para mais informações, consulte a documentação e os exemplos oficiais.

## Títulos

**Exemplo:**

```js title="apps/web/src/index.tsx"
import { App } from './App';
```

**Código:**

````mdx
```js title="apps/web/src/index.tsx"
import { App } from './App';
```
````

## Destaque de código

**Exemplo:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Código:**

````mdx
```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Destaque de código inline

**Exemplo:**

O resultado de `[1, 2, 3].join('-'){:js}` é `'1-2-3'{:js}`.

**Código:**

```mdx
O resultado de `[1, 2, 3].join('-'){:js}` é `'1-2-3'{:js}`.
```

## Destaque inline sensível ao contexto

Por exemplo, se você tiver o seguinte bloco de código:

```js
function getStringLength(str) {
  return str.length;
}
```

Quando nos referimos a `getStringLength{:.entity.name.function}` como uma função,
podemos colori-la como tal. O mesmo vale para `function{:.keyword}`, ou
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. Isso permite
vincular semanticamente o código inline ao bloco de código mais próximo ao qual ele se refere.

**Código:**
````mdx
Quando nos referimos a `getStringLength{:.entity.name.function}` como uma função,
podemos colori-la como tal. O mesmo vale para `function{:.keyword}`, ou
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. Isso permite
vincular semanticamente o código inline ao bloco de código mais próximo ao qual ele se refere.
````

## Destaque de palavras

**Exemplo:**

```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```

**Código:**
````mdx
```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```
````

## Destaque de linhas

**Exemplo:**

```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Código:**

````mdx
```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Numeração de linhas

**Exemplo:**

```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Código:**
````mdx
```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Diff

**Exemplo:**

```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

**Código:**

````mdx
```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```
````

## Destaque ANSI

**Exemplo:**

```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

ANSI inline: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`

**Código:**

````mdx
```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

ANSI inline: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`
````

## Destaque de grupos de palavras por id

Coloque um id depois de `#` após as palavras.
Isso permite colorir caracteres de formas diferentes com base no id informado.

**Exemplo:**

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

**Código:**
````md
```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```
````

Para alterar ou adicionar novos estilos:

```css title="apps/web/src/styles/mdx.css"
[data-chars-id='v'] {
  @apply !text-pink-300 bg-rose-800/50 font-bold;
}
```
