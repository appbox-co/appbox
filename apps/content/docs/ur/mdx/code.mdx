---
title: Code
description: اپنی MDX files میں code استعمال کرنے میں مہارت حاصل کریں
links:
  source: https://raw.githubusercontent.com/rehype-pretty/rehype-pretty-code/master/examples/next/src/app/index.mdx
sort: 1
---

`opendocs` MDX files میں code handle کرنے کے لیے [`rehype-pretty-code`](https://github.com/atomiks/rehype-pretty-code) library استعمال کرتا ہے۔ اس سے آپ code کے display ہونے کا طریقہ کافی حد تک customize کر سکتے ہیں!

مزید information کے لیے official documentation اور examples دیکھیں!

## Titles

**مثال:**

```js title="apps/web/src/index.tsx"
import { App } from './App';
```

**Code:**

````mdx
```js title="apps/web/src/index.tsx"
import { App } from './App';
```
````

## Code highlighting

**مثال:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**

````mdx
```js
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Inline code highlighting

**مثال:**

`[1, 2, 3].join('-'){:js}` کا result `'1-2-3'{:js}` ہے۔

**Code:**

```mdx
The result of `[1, 2, 3].join('-'){:js}` is `'1-2-3'{:js}`.
```

## Context-aware inline code highlighting

مثال کے طور پر، اگر آپ کے پاس code کا یہ block ہو:

```js
function getStringLength(str) {
  return str.length;
}
```

جب ہم `getStringLength{:.entity.name.function}` کو function کے طور پر refer کرتے ہیں،
تو اسے اسی طرح color کر سکتے ہیں۔ اسی طرح `function{:.keyword}`، یا
`str{:.variable.parameter}` بمقابلہ `str{:.variable.other.object}` وغیرہ۔ یہ inline code کو اس nearest code block سے semantically link کرنے دیتا ہے جس کا وہ حوالہ دیتا ہے۔

**Code:**
````mdx
When we refer to `getStringLength{:.entity.name.function}` as a function,
we can color it as such. Same with `function{:.keyword}`, or
`str{:.variable.parameter}` vs. `str{:.variable.other.object}`, etc. This allows
semantically link inline code to the nearest block of code it refers to.
````

## Word highlighting

**مثال:**

```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```

**Code:**
````mdx
```css /pl-12/ /border/
relative w-full pl-12 rounded-lg border
```
````

## Line highlighting

**مثال:**

```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**

````mdx
```js {2-4,6}
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Line numbering

**مثال:**

```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```

**Code:**
````mdx
```js showLineNumbers
module.exports = {
  theme: {
    extend: {
      colors: {
        warning: "hsl(var(--warning))",
        "warning-foreground": "hsl(var(--warning-foreground))",
      },
    },
  },
}
```
````

## Diff

**مثال:**

```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

**Code:**

````mdx
```diff
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```
````

## ANSI highlight

**مثال:**

```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`

**Code:**

````mdx
```ansi
[0;36m  vite v5.0.0[0;32m dev server running at:[0m

  > Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m
  > Network: [0;2muse `--host` to expose[0m

  [0;36mready in 125ms.[0m

[0;2m8:38:02 PM[0m [0;36;1m[vite][0m [0;32mhmr update [0;2m/src/App.jsx
```

Inline ANSI: `> Local: [0;36mhttp://localhost:[0;36;1m3000[0;36m/[0m{:ansi}`
````

## id کے ذریعے word group highlighting

Words کے بعد `#` کے بعد id لگائیں۔
یہ given id کی بنیاد پر characters کو مختلف رنگ دینے کی اجازت دیتا ہے۔

**مثال:**

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

**Code:**
````md
```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```
````

نئے styles change یا add کرنے کے لیے:

```css title="apps/web/src/styles/mdx.css"
[data-chars-id='v'] {
  @apply !text-pink-300 bg-rose-800/50 font-bold;
}
```
